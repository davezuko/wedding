{
  "name": "wedding",
  "version": "1.0.0",
  "main": "src/main.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "mkdir dist && cp -r src/. dist && cp -r public/. dist",
    "make": "yarn clean && yarn compile"
  },
  "dependencies": {
    "normalize.css": "^8.0.0",
    "preact": "^8.2.7"
  }
}
